[project]
name = "tgearn-demo"
version = "0.1.0"
description = "Two independent, access-restricted Telegram demonstrations using synthetic goods"
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
target-version = "py311"
line-length = 110
